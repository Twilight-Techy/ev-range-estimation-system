#include "model.h"

// IMPORTANT: Replace this ENTIRE file with the output of the following command:
// xxd -i lstm_model_quantized.tflite > model.cpp
//
// NOTE: Make sure the array name matches 'lstm_model_quantized_tflite'
// and 'lstm_model_quantized_tflite_len'. Add the 'alignas(8) const' modifier to the array
// for proper ESP32 memory alignment!

alignas(8) const unsigned char lstm_model_quantized_tflite[] = {
  // Array contents will go here
};
const unsigned int lstm_model_quantized_tflite_len = 0;
